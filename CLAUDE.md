[뱅크샐러드 브랜드 리디자인] AGENTS.md
이 문서는 AI 에이전트(Claude Code 등)가 뱅크샐러드 브랜드 리디자인 및 웹 랜딩 페이지 개발 프로젝트를 진행할 때 준수해야 하는 작업 수행 가이드라인과 규칙을 정의합니다.

1. 에이전트 기본 역할 및 작업 흐름
사전 분석 (Context First): 작업 착수 전 PRD.md, RULES.md(작업 규칙), PROJECT_CONTEXT.md 및 피그마 디자인 에셋을 먼저 분석합니다.

계획 수립 (Planning): 2개 이상의 파일을 수정하거나 주요 기능/스타일을 변경할 때는 항상 [수정 계획]을 먼저 제시합니다.

안전한 구현 (Safe Implementation): 명시된 기술 스택(Vanilla HTML/CSS/JS) 및 명명 규칙(snake_case, camelCase, is_active 등)을 철저히 준수하여 코드를 작성합니다.

검증 및 보고 (Validation & Report): 구현 완료 후 크롬 개발자 도구 기준 검증(360px/768px/1280px 반응형, 콘솔 오류, 접근성 포커스)을 진행하고 PROJECT_CONTEXT.md에 결과를 기록합니다.

2. Claude Code 전용 규칙
구현 전 계획 작성: 여러 파일을 수정하거나 컴포넌트 구조를 변경하는 작업은 변경 범위, 영향도, 작업 순서가 포함된 계획을 먼저 작성합니다.

사전 에셋/패턴 확인: 코드 작성 전에 기존 파일 구조(css/, js/), 기존 공통 스타일 변수, 실제로 추출되어 준비된 에셋(assets/ 내 SVG/PNG)을 반드시 확인하고 재사용합니다.

디자인 변경 시 선보고: 피그마 디자인 시안과 다른 기술적/구조적 결정을 내려야 하는 경우, 이유와 영향도를 사용자에게 먼저 보고하고 승인을 얻습니다.

공통 규칙 및 Skill 보완 제안: 동일한 스타일 오류나 스크립트 이슈가 반복될 경우, 공통 규칙 업데이트나 에이전트 Skill 보완 필요성을 보고합니다.

상태 문서 동기화: 작업 완료 후 PROJECT_CONTEXT.md 파일에 [완료된 내용], [남은 문제점], [검증 결과]를 즉시 갱신합니다.

3. 코드 작업 시 지격(Strict) 준수 사항
기술 스택 제약: React, Vue, TypeScript, Tailwind CSS 등 승인되지 않은 프레임워크나 외부 패키지를 임의로 추가하지 않습니다.

디자인 임의 재해석 금지: 피그마 시안에 없는 임의의 장식 요소, 가상의 3D 아이콘, 과도한 애니메이션을 추가하지 않습니다.

네이밍 규칙 엄수:

HTML ID & CSS Class: snake_case (상태: is_active, is_open, is_selected / 오류: has_error)

JS 변수 & 함수: camelCase (불리언: is, has, can, should / 이벤트: handleXxx)

가로 스크롤 방지: 모바일(360px), 태블릿(768px), 데스크톱(1280px) 모든 구간에서 overflow-x 스크롤이 발생하지 않도록 제어합니다.

4. 검증 및 결과 보고 양식
작업을 완료한 후에는 항상 아래 형식에 맞추어 결과를 보고합니다.

Markdown
### 뱡크샐러드 작업 완료 보고서

1. **수정 및 생성된 파일 목록:**
   - `index.html` (메인 랜딩 페이지 구조 수정)
   - `css/main.css` (신전 로고 모티프 반응형 레이아웃 스타일 추가)

2. **주요 구현 내용:**
   - [구현 기능 1]
   - [구현 기능 2]

3. **디자인/기술적 주요 판단 사항:**
   - (예: 피그마 내 SVG 에셋 `ic_bank_temple.svg` 우선 재사용 적용)

4. **검증 결과:**
   - [x] 크롬 콘솔 오류 없음 (404/스크립트 에러 0건)
   - [x] 360px, 768px, 1280px 반응형 가로 스크롤 없음 확인
   - [x] 키보드 Focus (`:focus-visible`) 정상 표시 확인

5. **확인하지 못한 부분 / 추후 논의 필요 사항:**
   - (해당 사항 작성)