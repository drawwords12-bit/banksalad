[뱅크샐러드 브랜드 리디자인] Project Rules
1. 사전 문서 및 가이드라인 숙지
모든 작업 착수 전, PRD.md와 AGENTS.md를 반드시 정독하고 요구사항과 작업 절차를 확인합니다.

작업 중 판단이 애매한 경우 임의로 진행하지 않고 문서 내 명시된 기준을 우선적으로 따릅니다.

2. 디자인 및 코드 기반 사실(Facts) 우선
피그마(Figma) 디자인 원본과 기존 코드베이스에서 직접 확인된 사실(컬러 토큰, 마진/패딩, 폰트 스펙, 추출된 에셋 등)을 최우선 기준으로 준수합니다.

추측에 의존한 스타일링이나 가상의 에셋 생성을 금지합니다.

3. PRD 정의 범위 엄수
PRD.md에 정의되지 않은 새로운 화면, 비즈니스 로직, 임의의 장식용 그래픽 및 레이아웃을 추가하지 않습니다.

요청된 뱅크샐러드 랜딩 페이지 및 로고 인터랙션 범위 내의 기능 구현에 집중합니다.

4. 기존 폴더 구조 및 자원 재사용
프로젝트 내 기존 폴더 구조(css/, js/, assets/ 등), 공통 스타일(reset.css, common.css), 디자인 토큰(CSS 변수)을 최우선으로 확인하고 재사용합니다.

assets/ 폴더 내 이미 존재하거나 추출된 이미지/아이콘(ic_bank_temple, logo_symbol 등)을 재선언 없이 활용합니다.

5. Figma MCP 정밀 검증
Figma MCP 활용 시 레이어 구조, 스크린샷, 디자인 변수, 실제 다운로드 에셋을 다각도로 종합 확인합니다.

피그마의 픽셀 단위 레이아웃 오차와 실제 웹 브라우저 랜더링 간의 차이를 점검합니다.

6. 순수 웹 기술(Vanilla Stack) 변환 적용
AI 에이전트 또는 Figma MCP에서 자동 생성된 React, Vue, Tailwind CSS 예시 코드는 프로젝트의 순수 기술 스택(HTML5, CSS3, Vanilla JavaScript ES6+) 구조에 맞게 반드시 다듬어 재작성합니다.

외부 프레임워크나 유틸리티 CSS 라이브러리를 추가하지 않습니다.

7. 반응형 해상도 & 접근성 필수 검증
360px(모바일), 768px(태블릿), 1280px(데스크톱) 모든 분기점(Breakpoints)에서 레이아웃 잘림 및 가로 스크롤(overflow-x) 발생 여부를 정밀 확인합니다.

키보드 Focus(:focus-visible), Tab 키 이동 순서, 버튼/링크 시맨틱 태그 준수 여부를 검증합니다.

8. 작업 상태 문서(PROJECT_CONTEXT.md) 동기화
단위 작업 및 전체 구현이 완료된 후, PROJECT_CONTEXT.md 파일에 아래 3가지 항목을 필수 기록합니다:

완료된 구현 내용

남아있는 문제점 및 개선 과제

최종 반응형/접근성/콘솔 검증 결과